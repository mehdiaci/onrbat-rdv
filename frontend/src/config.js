// Base URL de l'API. En production sur le même domaine, laisser vide.
// Pour déploiement séparé (ex: API sur api.mondomaine.com), définir VITE_API_URL.
export const API_BASE = import.meta.env.VITE_API_URL ?? ''
